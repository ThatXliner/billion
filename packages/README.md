# Billion

## Components

- `scraper/` a scraper to grab executive data periodically
- `backend/` (Rust? tRPC? probably tRPC) backend infra to manage server and other generation pipelines
- `mobile/` React Native frontend


 this is structured as a monorepo ngl
